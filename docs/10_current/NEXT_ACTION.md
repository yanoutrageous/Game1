# Next Action

文档状态：M7 合并且 ART24R2 失败封存后的后续选择边界；最后更新 2026-07-20。

## 当前状态

M7“完整地图难度与首轮长期成长内容”已经完成并合并到当前美术分支。ART24R2 已按用户指令停止继续扩大返工，以 `FAIL (24/61 PASS)` 的冻结审计结果结束并 push；当前没有自动开启的后继阶段。

## 后续计划必须继承

- 以当前工作分支最新内容为准，不以 `main` 推断项目状态。
- 程序基线是 ART23 + G41 + M6 + M7；ART24R2 已失败封存并排除于合格美术基线，合格美术基线仍为 ART23。
- `RunAssetLedger` 是局内物品位置权威，MetaProgress 只消费最终结算快照。
- 失败确认前不能写局外；同一 `result_id` 不能重复提交。
- 消耗品终局清除、2 件装备、3 个携入消耗品、背包 10、失败保全 4 是当前已固定规则。
- 并行美术继续使用稳定显示键、状态和锚点，贴图尺寸不得决定碰撞、容量或结算。
- 八档地图、委托、任务、成就、研究、图鉴、资历、收藏和红点已经是真实内容；后续不得退回预览状态或只读占位。

## 可讨论的下一阶段方向

1. 只有用户明确重新开启 ART24R2 时，才沿原冻结标准补验并修正剩余 37 个失败状态；优先失败保全、地图返回、多物品/离开隐藏、怪物与减弱动效。
2. 扩充局内内容量：新事件、敌人、精英 / Boss、装备被动与更深地图差异，并进行完整人工长时间游玩。
3. 扩展仓库与经济：整理、堆叠、扩容、保险、寄售、批量出售及更完整的购买 / 出售规则。
4. 增加跨进程局内检查点恢复，使当前进程续玩升级为退出程序后续玩。
5. 规划抽奖、唯一物真实获取、实际外观和收藏奖励；在设计规则明确前继续保持锁定。

这些方向都不是自动授权；下一步先由用户选择阶段目标和范围。

# Next Action

文档状态：I0 + ART21 整合后的下一步边界
最后更新：2026-07-16

## 当前授权状态

I0 与 ART21 基线整合完成后，当前没有已授权的后续 active stage。
不得仅根据本文件自动启动 ART22、代码重构、玩法扩展、CI 或发布工作。

## 下一阶段候选

后续应由用户单独命名和授权，可从以下独立方向中选择：

1. 主菜单后续动效素材升级与更高帧数重绘。
2. 可见 Godot 启动日志完全隔离门。
3. 最小 CI / export / release gate。
4. Persistence 往返、失败恢复和版本迁移可靠性。
5. 继续按特征测试保护缩小 RunScene。
6. MVP 内容闭环、平衡、引导与完整人工游玩。

## 必须继承

- I0 的锁定工具链、隔离 harness、污染守卫和声明边界。
- ART21 的主菜单场景、资产契约、路由和 motion contract。
- `git rev-parse --show-toplevel` 的当前路径权威。
- I0 历史安全不符合项与有限人工覆盖记录。
- 不使用固定 `D:\AGAME1` 作为当前配置。

## 验收边界

本次 12/12 runner 与 ART21 runtime PASS 不等于完整 gameplay、
人工游玩、最终视觉、性能、CI、导出或发布 PASS。

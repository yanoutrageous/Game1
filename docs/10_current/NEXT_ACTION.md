# Next Action

文档状态：M6 关闭后的下一步决策边界；最后更新 2026-07-19。

## 当前状态

M6“真实资产、出勤与结算闭环”已经完成实现与自动化验证。当前没有自动授权的后续阶段；下一阶段仍应先与用户完成计划，再按“计划 → 计划审计 → 执行切片 → 执行审计 → 上传”推进。除用户专门说明外，不提前自行发起阶段审计。

## 后续计划必须继承

- 以当前工作分支最新内容为准，不以 `main` 推断项目状态。
- 程序基线是 ART23 + G41 + M6；ART24 明确排除。
- `RunAssetLedger` 是局内物品位置权威，MetaProgress 只消费最终结算快照。
- 失败确认前不能写局外；同一 `result_id` 不能重复提交。
- 消耗品终局清除、2 件装备、3 个携入消耗品、背包 10、失败保全 4 是当前已固定规则。
- 并行美术继续使用稳定显示键、状态和锚点，贴图尺寸不得决定碰撞、容量或结算。

## 可讨论的下一阶段方向

1. 补齐最小目标、奖励与任务实例，使“出发目标—局内完成—结算奖励”成为真实闭环。
2. 扩展仓库实际操作，例如出售、整理、堆叠和容量，但继续避免一次进入完整经济系统。
3. 增加跨进程局内检查点恢复，使当前进程续玩升级为退出后续玩。
4. 扩充事件、敌人、装备被动和数值内容，并进行完整人工长时间游玩。
5. 在程序接口稳定后接收美术侧同期成果，单独规划表现整合与视觉验收。

这些方向都不是自动授权；下一步先由用户选择阶段目标和范围。

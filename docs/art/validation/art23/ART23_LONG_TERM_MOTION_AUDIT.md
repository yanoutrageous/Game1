# ART23 长期系统动效审计

状态：自动化契约 PASS；`ART23-CU-FROZEN-2` Computer Use 连续观察 60 秒 PASS。

## 静态与运行时证据

- 角色使用 ART21 同一角色的 8 张唯一纹理，不生成新的违和角色。
- `CHARACTER_IDLE_SEQUENCE` 使用 0–5 帧，0.34 s 节拍；`CHARACTER_LOOK_SEQUENCE` 使用 6–7 帧，首次 5 s、后续约 10 s 间隔，0.42 s 节拍。
- 一级家具切换包含 `CLOSING → CLOSED → SWITCHING → OPENING → OPEN`，支持快速操作的 latest-request-wins。
- 二级页的底板、标题、正文、元信息、卡片共用淡入，避免单独文字漂浮。
- 环境动效由暖灯/蓝灯透明度呼吸和两组低密度粒子组成，创建顺序位于 UI 组件之前，不覆盖右侧档案与文字。
- 减少动态效果会冻结过程动画并采用立即切换。

## 失败条件

- 角色只在两帧间高频切换。
- 文字在羊皮纸或卡片出现前独立显示。
- 家具切换后选中模块与实际家具不同。
- 收起时右侧档案或顶部模块随中央层移动。
- 灯光/粒子遮挡文字、穿过档案框或形成悬空光球。

任一失败均按冻结标准全量返工，不允许以静态截图替代 Computer Use 的连续观察。

## Computer Use 最终观察

2026-07-18 从真实主菜单进入长期系统，在最终代码上连续观察 60 秒，并以约 0.42 秒间隔追加抽查角色资源帧。观察到常态呼吸、姿态变化、眨眼/观察动作与回归常态；暖/蓝低透明呼吸光和低密度粒子持续存在，没有穿过右侧档案或遮挡正文。羊皮纸、标题、说明、元信息和卡片在整个观察期保持同组稳定，没有文字独立浮动。结果：PASS。

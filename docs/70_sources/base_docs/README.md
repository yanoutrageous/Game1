# Base Docs Sources

文档状态：外部来源入口
适用范围：Base Docs 当前归档来源与冻结历史快照
最后更新：2026/07/23

## I3 当前仓库内原始策划基线

用户已在 I3 明确授权将 `sources.zip` 中的原始策划案经审计后纳入仓库。当前原件位于：

```text
sources/base/原始策划案/
```

25 份文件保持原名、完整正文和源字节；不在 `repo/docs` 复制正文。精确 SHA 与字节数见
`sources/base/manifests/ORIGINAL_PLANNING_MANIFEST.csv`，相互关系、当前用途与保留说明见
`docs/70_sources/base_docs/I3_ORIGINAL_PLANNING_RELATIONSHIP_REGISTRY.md`。

以下 G40/DOC-GOV-001 内容保留为历史外部来源说明；它不覆盖 I3 的新授权，也不把
历史固定盘符恢复为当前路径权威。

来源注册表：

```text
docs/70_sources/base_docs/BASE_DOCS_SOURCE_REGISTRY.md
```

当前策划原件只读来源：

```text
D:\AGAME1\Base Docs
```

`source_snapshot/` 是 2026/06/22 在当时授权下生成的冻结历史快照。它不自动同步，不替代当前 Base Docs，不证明旧文件名仍是唯一有效路径。无新授权不得刷新、补齐或新增副本。

读取当前策划资料时，应先只读扫描外部 Base Docs，并按主题、相近名称、更新时间和文档状态定位当前归档文件。

## DOC-GOV-001 规则

```text
1. Base Docs 是用户注入内容 / 外部策划原件 / 个人留档区。
2. Base Docs 不参与仓库去重。
3. 仓库不得覆盖、移动、删除、重命名或改写 Base Docs 原件。
4. Base Docs_Governance 是外部治理快照区，不替代当前仓库事实源。
5. 仓库内契约应引用来源，不把外部未确认内容写成定案。
```

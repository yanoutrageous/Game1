# ART24R2 未通过验收封存交接

## 状态

- 分支：`art/art24r2-g41-m6-combat-ui`
- 日期：2026-07-19
- 结论：`FAIL / STAGE_ARCHIVED_BY_USER_DIRECTION`
- Computer Use：24/61 二级状态通过
- 合格美术基线：仍为 ART23

## 本轮交付内容

- 按 UE 代码与实机共同证据重排局内 HUD：方形主房间、合理角色比例、左栏、紧凑协议层与底部热键栏。
- 重做展开地图、背包和成功/失败结算的尺寸权重与层级。
- 地面物品改为世界实体优先、靠近显示上下文；箱子共享同一近距逻辑并支持重复开合。
- 接入 G41/M6 真实物品、容量、失败保全与结算状态；补齐物品/怪物/特效资源映射和可替换生成变体。
- 增加 5 档分辨率、61 状态契约、真实主场景坐标转换与 UI 场景探针。

## 为什么没有通过

最终 Computer Use 尚未覆盖或通过 37 个冻结状态，主要集中在失败保全四态、地图已探索点返回、多地面物与离开隐藏、事件/撤离、怪物动作和减弱动效。G41 正式包装校验还保留美术路径审计失败，背包场景探针退出存在资源未释放警告。

用户明确要求停止继续扩大返工，以“验收部分不通过”的状态结束并 push。因此本交接只封存当前成果，不声明 ART24R2 完成。

## 恢复入口

1. 读取 `docs/validation/ART24R2_FINAL_COMPUTER_USE_RESULTS.md` 中的 37 个 `FAIL`。
2. 沿用 `ART24R2-FINAL-CU-FROZEN-1`，不得重新放宽验收标准。
3. 优先完成失败保全、地图返回、多物品/离开隐藏、怪物与减弱动效。
4. 修复 G41 包装校验的 ArtVisual 审计标记与硬编码最终美术路径。
5. 清理场景探针退出时的 `ObjectDB/resources still in use` 警告。
6. 复验所有受影响状态和相邻状态；61/61 通过前仍不得升级为合格美术基线。

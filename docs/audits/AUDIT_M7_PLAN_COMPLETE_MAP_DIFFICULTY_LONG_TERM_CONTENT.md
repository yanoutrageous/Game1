# M7 计划审计：完整地图难度与首轮长期成长内容

审计状态：PASS_WITH_NOTES
审计日期：2026-07-19
审计对象：`docs/20_product/M7_COMPLETE_MAP_DIFFICULTY_LONG_TERM_CONTENT_CONTRACT.md`

## 审计结论

```text
Scope: valid_stage
Risk: high
Action: execute_by_slice
Evidence: C（策划源、当前代码与阶段契约）
Claim check: accurate
Debt: non-blocking（实现与可见验证尚待执行）
Next: 进入 M7 执行；每个高风险切片先做路径与脏状态门禁。
```

M7 跨越地图生成、出发配置、局内事件、结算、存档和长期系统，单独任何一项都不足以完成用户要求的“程序侧补全”。当前计划通过八个切片限制执行面，因此虽然规模大，但不是不可执行的 `too_broad` 阶段。

## 事实核对

1. 地图数量、雷房与内容房数量来自当前地图 / 难度策划案：7×7 为 10 / 5 / 5 / 5，10×10 为 20 / 10 / 10 / 10，13×13 为 34 / 17 / 17 / 17。
2. 八档撤离点配置与策划案一致；7×7 简单的“可见”已明确为只显示位置、不自动探索路径。
3. 当前 `TruthMap` 使用可变宽高、按种子生成、互斥候选池和八邻域雷数，能够承接 7×7、10×10、13×13，不需要重做地图领域。
4. 当前小地图按宽高动态生成，但 13×13 需要调整最小格尺寸和三个目标分辨率的可见验证。
5. M6 已具备精确实例、携入、失败确认、最终结算和结果幂等，可作为奖励与长期增量的唯一提交边界。
6. ART23 六模块容器已经存在，但其模型明确标注 preview / display-only；M7 必须逐项替换为真实状态，不能以现有页面存在作为验收。

## 执行硬门禁

- 奖励与进度不得由 UI 直接写入。
- 失败抢救确认前不得写入局外资产或长期进度。
- 八档地图必须全部可通过正常流程解锁和进入。
- 13×13 必须通过局内可见验证，不能只通过生成器测试。
- 旧 M6 存档迁移必须保留金币、仓库和历史。
- ART24 / ART24R1 不得进入基线或变更集。
- 不修改共享美术源、导入元数据、资源标识、共享场景和项目设置；若确实需要，停止并重新审计。
- 提交与上传前重新核对分支、HEAD、origin、脏状态和 diff 范围。

## 审计备注

- 数值是首轮可玩值，不代表最终平衡认证。
- 可见 UI、完整玩家路径和持久化属于 M 阶段完成声明的必要证据；纯静态检查不足以关闭 M7。
- 现有 Godot 退出清理诊断若仍出现，应按真实状态记录为非阻断债务，不得改写成“无警告”。
